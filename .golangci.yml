version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - misspell
  settings:
    errcheck:
      check-blank: false
    misspell:
      locale: US

formatters:
  enable:
    - gofmt
    - goimports


